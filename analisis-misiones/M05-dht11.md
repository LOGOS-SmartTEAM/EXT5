<!-- ═══════════════════════════ INICIO MISIÓN M05 ═══════════════════════════ -->
<!-- Cursor: ejecutá SOLO esta misión. Requiere M00 y MD (necesita _referencia/). -->

# MISIÓN M05 — DHT11 · Humedad y Temperatura (B05)

- **Repo objetivo:** `EXT5` · **Archivo a crear:** `blocks/sabana/dht11.ts`
- **Diseño:** `STV2-PADRE/blocks/sabana/dht11.ts` (hoy es un stub `return 0`)
- **Código:** `_referencia/ext5-DTH11.ts` → `leerDHT11()`
- **Depende de:** M00 (`puertoToDigitalPin`), MD (archivado de `_referencia/`)
- **Estado:** ⬜ pendiente

---

## 1. Confirmación de CATEGORÍAS, TEXTOS y VARIABLES

| Propiedad | Valor |
|---|---|
| `blockId` | `sabana_dht11` |
| **Texto** | `%dato │ en pin %puerto` |
| **Group** | `SENSORES` |
| **Color** | `#FFB800` (GPIO) |
| `weight` / `blockGap` | `90` / `8` |
| Forma | redondo (reporter, `number`) |

| Parámetro | Tipo | Desplegable | Valores internos |
|---|---|---|---|
| `dato` | `SabanaDatoDHT11` | `Humedad` / `Temperatura` | `0` / `1` |
| `puerto` | `SabanaPuerto` | `P0` `P1` `P2` `P3` | `0`–`3` |

**Retorno:** `number` — **con decimales** (ej. `23.4`). Humedad en % relativo,
temperatura en °C. Devuelve **`-999`** si el sensor no responde o falla el checksum.

> ✅ **Cero cambios de diseño.** El texto, el color GPIO y el desplegable de pin de
> PADRE son correctos. Solo se escribe el cuerpo de la función.

---

## 2. Decisión resuelta: el DHT11 es GPIO, no I2C

En una etapa anterior del análisis se evaluó pasar este bloque a I2C (quitar el
`%puerto`, color a `#35BFE9`), porque `original-alex` tiene un driver I2C en la
dirección `0x27`. **Esa decisión quedó cancelada.**

| | ORIGINAL (I2C `0x27`) | **EXT5 (1 hilo, GPIO)** ✅ |
|---|---|---|
| Conexión | bus I2C | **pin digital del puerto** |
| Protocolo | leer 5 bytes de un registro | **40 bits con temporización en µs** |
| Validación | ninguna | **checksum de 8 bits** |
| Resolución | 1 unidad (un byte) | **decimales** (`23.40`) |
| Manejo de error | ninguno | **`-999`** |

El driver de EXT5 es netamente superior y corresponde al módulo DHT11 físico de
3 pines (VCC / DATA / GND). Por lo tanto el `%puerto` y el color `#FFB800` de
PADRE **ya son correctos y no se tocan**.

### Y los valores del enum también son correctos

`leerDHT11()` devuelve un array `[humedad, temperatura]`. Los valores del enum de
PADRE (`Humedad = 0`, `Temperatura = 1`) coinciden exactamente con los índices de
ese array, así que el cuerpo puede ser `return lectura[dato]` sin ninguna
traducción.

> ⚠️ **No renumerar el enum.** Los valores `0` y `1` son los índices del array de
> lectura. Invertirlos hace que "Humedad" devuelva temperatura, y el código sigue
> compilando sin ningún aviso.

---

## 3. Estado actual del stub en PADRE

```ts
namespace bloques {
    export enum SabanaDatoDHT11 {
        //% block="Humedad"
        Humedad = 0,
        //% block="Temperatura"
        Temperatura = 1,
    }

    //% blockId=sabana_dht11
    //% block="%dato │ en pin %puerto"
    //% group="SENSORES" color="#FFB800" weight=90 blockGap=8
    export function dht11(dato: SabanaDatoDHT11, puerto: SabanaPuerto): number {
        return 0
    }
}
```

---

## 4. Resultado esperado

Crear `blocks/sabana/dht11.ts` con este contenido completo:

```ts
namespace bloques {

    // ── Constantes del protocolo DHT11 ───────────────────────────────
    const DHT11_BITS = 40                // 5 bytes
    const DHT11_START_LOW_MS = 18        // señal de inicio en bajo
    const DHT11_POST_START_US = 40       // espera tras liberar el pin
    const DHT11_BIT_THRESHOLD_US = 28    // > 28us en alto = bit "1"
    const DHT11_ERROR = -999             // sensor no responde o checksum inválido

    export enum SabanaDatoDHT11 {
        //% block="Humedad"
        Humedad = 0,
        //% block="Temperatura"
        Temperatura = 1,
    }

    /**
     * Lee humedad y temperatura crudas del DHT11 por protocolo de un hilo.
     * Devuelve [humedad, temperatura], o [-999, -999] si falla.
     *
     * Origen: _referencia/ext5-DTH11.ts -> leerDHT11(), adaptado a su vez de
     * la extensión pública alankrantas/pxt-DHT11_DHT22 (MIT).
     *
     * ADVERTENCIA: la temporización de esta función es crítica. Los waitMicros
     * y los while de espera activa NO se pueden reemplazar por basic.pause()
     * ni reordenar: el protocolo del DHT11 se codifica en microsegundos.
     */
    function leerDHT11(pin: DigitalPin): number[] {
        let dataArray: boolean[] = []
        let resultArray: number[] = [0, 0, 0, 0, 0]
        for (let i = 0; i < DHT11_BITS; i++) dataArray.push(false)

        // señal de inicio
        pins.digitalWritePin(pin, 0)
        basic.pause(DHT11_START_LOW_MS)
        pins.setPull(pin, PinPullMode.PullUp)
        pins.digitalReadPin(pin)
        control.waitMicros(DHT11_POST_START_US)

        if (pins.digitalReadPin(pin) == 1) {
            return [DHT11_ERROR, DHT11_ERROR]   // el sensor no respondió
        }

        while (pins.digitalReadPin(pin) == 0);  // respuesta del sensor
        while (pins.digitalReadPin(pin) == 1);  // respuesta del sensor

        // lectura de 40 bits (5 bytes)
        for (let i = 0; i < DHT11_BITS; i++) {
            while (pins.digitalReadPin(pin) == 1);
            while (pins.digitalReadPin(pin) == 0);
            control.waitMicros(DHT11_BIT_THRESHOLD_US)
            // si sigue en alto pasado el umbral, es un bit "1"
            if (pins.digitalReadPin(pin) == 1) dataArray[i] = true
        }

        for (let i = 0; i < 5; i++)
            for (let j = 0; j < 8; j++)
                if (dataArray[8 * i + j]) resultArray[i] += 2 ** (7 - j)

        let checksumTmp = resultArray[0] + resultArray[1] + resultArray[2] + resultArray[3]
        if (checksumTmp >= 512) checksumTmp -= 512
        if (checksumTmp >= 256) checksumTmp -= 256

        if (checksumTmp != resultArray[4]) {
            return [DHT11_ERROR, DHT11_ERROR]   // checksum inválido
        }

        const humedad = resultArray[0] + resultArray[1] / 100
        const temperatura = resultArray[2] + resultArray[3] / 100
        return [humedad, temperatura]
    }

    /**
     * STV2-3 — DHT11 (humedad / temperatura) en un puerto GPIO.
     *
     * Devuelve el valor CON DECIMALES, o -999 si el sensor no responde o si
     * falla la verificación de datos.
     *
     * El módulo es de 3 pines (VCC / DATA / GND) y usa protocolo de un hilo
     * sobre el pin digital del puerto. NO es I2C.
     *
     * Los valores del enum (Humedad = 0, Temperatura = 1) son los índices del
     * array que devuelve leerDHT11(). NO renumerarlos.
     *
     * @param dato Humedad o Temperatura, eg: SabanaDatoDHT11.Humedad
     * @param puerto puerto GPIO de la placa, eg: SabanaPuerto.P0
     */
    //% blockId=sabana_dht11
    //% block="%dato │ en pin %puerto"
    //% group="SENSORES" color="#FFB800" weight=90 blockGap=8
    export function dht11(dato: SabanaDatoDHT11, puerto: SabanaPuerto): number {
        const lectura = leerDHT11(puertoToDigitalPin(puerto))
        return lectura[dato]
    }
}
```

### Insertar en `pxt.json`

Agregar `"blocks/sabana/dht11.ts"` **después de** `"blocks/sabana/boton.ts"` y
**antes de** `"blocks/sabana/potenciometro.ts"`.

---

## 5. Criterios de aceptación

- [ ] El bloque aparece en `SENSORES`, **amarillo `#FFB800`**, texto
      `Humedad │ en pin P0`, con **los dos desplegables** (dato y pin).
- [ ] El desplegable de dato muestra `Humedad` y `Temperatura`, con **`Humedad`
      por defecto**.
- [ ] `Temperatura` devuelve un valor plausible de interior (aprox. **18–30**),
      **con decimales**.
- [ ] `Humedad` devuelve un valor plausible (aprox. **30–70**).
- [ ] ⚠️ **Prueba cruzada obligatoria:** soplar aire caliente cerca del sensor
      debe subir la **temperatura**, no la humedad. Si sube el valor de "Humedad",
      los valores del enum quedaron invertidos.
- [ ] Los dos datos devuelven **valores distintos**.
- [ ] Desconectando el sensor, el bloque devuelve **`-999`** en vez de colgarse
      o devolver basura.
- [ ] Funciona en los 4 puertos — verificar el **puerto 3** (pin P8).
- [ ] El proyecto compila.

### Prueba

```ts
basic.forever(function () {
    basic.showString("T")
    basic.showNumber(bloques.dht11(bloques.SabanaDatoDHT11.Temperatura, SabanaPuerto.P0))
    basic.pause(500)
    basic.showString("H")
    basic.showNumber(bloques.dht11(bloques.SabanaDatoDHT11.Humedad, SabanaPuerto.P0))
    basic.pause(2000)
})
```

> La pausa de 2 s no es decorativa: el DHT11 **no admite más de una lectura por
> segundo**. Ver riesgos.

---

## 6. Riesgos y advertencias

| Riesgo | Detalle / mitigación |
|---|---|
| **🔴 Tocar la temporización** | Los `control.waitMicros()` y los `while` de espera activa **son el protocolo**. Reemplazarlos por `basic.pause()`, reordenarlos o "optimizar" los `while` rompe la lectura por completo. Este código se copia tal cual. |
| **Renumerar el enum** | `Humedad = 0` y `Temperatura = 1` son los índices del array de retorno. Invertirlos hace que cada opción devuelva el dato del otro, sin ningún error de compilación. |
| **Frecuencia de lectura** | El DHT11 tolera **1 lectura por segundo** como máximo. Dentro de un `forever` sin pausa devuelve `-999` la mayoría de las veces. Documentar en el manual del alumno. |
| **Espera activa bloqueante** | Los `while` bloquean la micro:bit hasta ~25 ms por lectura. Si el sensor está desconectado, el primer `if` corta antes de entrar a los `while`, así que **no se cuelga**. Pero no llamar a este bloque dentro de un manejador de evento. |
| **Pasarlo a I2C** | Ya se evaluó y se descartó (sección 2). El módulo del aula es de 3 pines. |
| **`-999` visible para el alumno** | Es el valor de error heredado de EXT5. Si molesta pedagógicamente, cambiarlo es un cambio de diseño → otra misión. |
| **Colisión de modo de pin** | El DHT11 escribe y lee sobre el mismo pin. Si el puerto se comparte con otro componente, la lectura falla. Un puerto, un componente. |

---

<!-- ═══════════════════════════ FIN MISIÓN M05 ═══════════════════════════ -->
<!-- Fin de M05. Siguiente: M06 — Sensor de Color. -->
